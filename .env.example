# ── App ──────────────────────────────────────────────
NODE_ENV=development
PORT=3000
API_PREFIX=api/v1

# ── Database ─────────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=staynest
DB_PASSWORD=changeme
DB_NAME=staynest
DB_SSL=false

# ── Sentry ───────────────────────────────────────────
SENTRY_DSN=
SENTRY_ENVIRONMENT=development

# ── Paystack (Phase 1 — Stage 3) ────────────────────
# PAYSTACK_SECRET_KEY=sk_test_...
# PAYSTACK_PUBLIC_KEY=pk_test_...
# PAYSTACK_WEBHOOK_SECRET=whsec_...

# ── Cloudflare R2 (Phase 1 — Stage 1) ───────────────
# R2_ACCOUNT_ID=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET_NAME=staynest-media
# R2_PUBLIC_URL=https://media.staynest.app

# ── JWT (Phase 1 — Stage 1) ─────────────────────────
# JWT_SECRET=generate-a-real-one
# JWT_EXPIRES_IN=15m
# JWT_REFRESH_EXPIRES_IN=7d
